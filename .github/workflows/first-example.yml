name: My First Workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My first GitHub Action Job"

        - name: List all files
          run: ls

        - name: Read file
          run: cat README.md